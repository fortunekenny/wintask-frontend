import { styled } from "styled-components";

const Navbar = () => {
  return (
    <Wrapper>
      <h3>Navbar</h3>
    </Wrapper>
  );
};

const Wrapper = styled.div`
  background: skyblue;
`;

export default Navbar;
